<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_self_order.OrderNotePopup">
        <div class="self_order_order_note_popup o_dialog">
            <div role="dialog" class="modal d-block" tabindex="-1">
                <div class="modal-dialog modal-dialog-centered" role="document">
                    <div class="modal-content rounded-4 shadow">
                        <div class="modal-body p-4 d-flex flex-column gap-3">
                            <div class="d-flex justify-content-between align-items-center">
                                <h2 class="mb-0">Add an order note</h2>
                                <button class="btn-close" aria-label="Close" t-on-click="() => this.props.close()"/>
                            </div>
                            <textarea
                                class="form-control rounded-3 order-note-textarea"
                                rows="4"
                                t-model="state.note"
                                placeholder="Specify which utensils, napkins, straws, and condiments you want to be included or any special instructions that you want the restaurant to be aware of"
                            />
                            <div class="d-flex gap-2">
                                <button type="button" class="btn btn-primary flex-grow-1" t-on-click="() => this.confirm()">Add</button>
                                <button type="button" class="btn btn-secondary flex-grow-1" t-on-click="() => this.props.close()">Discard</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </t>
</templates>
